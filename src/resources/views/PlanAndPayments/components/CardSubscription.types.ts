export interface ICardSubscription {
    title: string;
    description: string;
    buttonText: string;
    buttonColor: string;
  }
  
export interface ICardSubscriptionProps extends ICardSubscription {}